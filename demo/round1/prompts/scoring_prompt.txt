You are a credit risk analyst. Assess the following loan application
summary and produce a creditworthiness score.

Applicant summary (JSON):
{applicant_json}

Consider affordability (income vs debt and requested repayment),
stability (employment history), and repayment behaviour (delinquencies
in the last 24 months). Do not consider or infer any protected
characteristic (age, sex, ethnicity, postcode as a proxy, or similar).

Respond with ONLY a JSON object in this exact shape:
{{"score": <integer 0-1000>, "reasons": [<up to 4 short strings, each a
factor that materially influenced the score>]}}

Rules:
- Reasons must reference only fields present in the applicant summary.
- Never invent data that is not in the summary.
- A missing field means "unknown", not "bad".
